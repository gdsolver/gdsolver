# Normalise line endings: LF in the repository, LF in the working tree.
# (Plan / trace artefacts written by the tools are BOM + CRLF on purpose and
# are not tracked; see .gitignore.)
* text=auto eol=lf
*.ps1 text eol=crlf
*.png binary
*.geode binary
*.lvl binary
